#pragma once

// Project SoftObjectPath.h — delegates to the engine's authoritative header.
#include <Core/Object/SoftObjectPath.h>

// FObjectRef and additional project-side declarations are here.
// (Previously in this file; now split)
