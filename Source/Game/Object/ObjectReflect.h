#pragma once

// Project ObjectReflect.h — extends engine's ObjectReflect with project-side registry implementations.
// The engine's ObjectReflect.h is the authoritative header for type definitions.

#include <Core/Object/ObjectReflect.h>

// Additional project-side declarations (if any) can be added below.
